[du-archive] 2026-09-02T220354+08:00 | role=assistant | blocks=reasoning,text
────────────
v0.2 全链路完成。三件交付对照你的三点需求：

**① 后端选稿（不碰文件）**：动态插件 `dudrft-3` 的 `GET /du-drafts` 已在线——列出 待发布/ 全部稿件，`?name=` 取原文，CORS 全开。今晚踩了两枚新坑顺手记录：动态环境**没有 URL 全局**（内建表外，pkg-3 因此 500，改手工解析 query）；fs 服务的 stat 不给 mtimeMs（全 0）——排序改文件名降序，日期前缀天然时序。另外「蒸馏提问件不在列表」排查过了：**你已把它移进已发布**（收稿动作），端点没病。

**② 专栏标题注入**：从 md 一级标题解析（自动剥掉「——回答 D₀」这类尾巴），native setter 写入标题框再补 input 事件（React 受控组件的正确姿势）；面板里标题可改，正文可编辑后注入。

**③ 回答页跨页流**：列表里点回答稿 → 脚本记住待注入稿（GM 存储）→ 自动打开 D₀ 里的问题网址（留网址的惯例保持）→ 新页面加载时自动 ensureEditor（编辑器不在就先点「写回答」按钮）→ 注入正文 → 清待办。**提问件按你裁定标「人工」不做注入**；发布按钮仍然只定位高亮，点击留给你。

需要你做的一步：Tampermonkey 里把脚本内容更新为 v0.2（整文件重贴）→ 开 zhuanlan.zhihu.com/write 试专栏全流程 → 开任意 question 页试回答跨页流。如果回答页的编辑器或「写回答」按钮没被找中，Console 里把实际 DOM 结构发我，我回填选择器——那是最后一块没实测的拼图。

隔夜改范围变更（提问退出，只改专栏和回答）已记日志 §十五。今天的账全在案，随时说收束。
