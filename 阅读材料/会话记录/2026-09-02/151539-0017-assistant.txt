[du-archive] 2026-09-02T151539+08:00 | role=assistant | blocks=reasoning,text
────────────
挂载完成，全部验证通过。汇报如下：

---

**[工作区] · du preset 诊断与手动挂载**

**一、需要挂载的清单（du preset 全貌）**

| # | 组件 | 功能 | 本次处置 |
|---|------|------|---------|
| 1 | du-clock v2 | 每条你的消息尾部追加 [时间锚点] | ✅ 动态挂载，待你下条消息验证 |
| 2 | du-quiet | 过滤 runtime context 的 sandbox/approval policy | ✅ 动态挂载，已证实（本回合 context=none） |
| 3 | du-archive | 会话消息增量归档 → 会话记录/日期/ | ✅ 动态挂载，已落 3 条 |
| 4 | dusync | /du-sync + /du-scan 同步端点（3080） | ✅ 动态挂载，HTTP 200 |
| 5 | du-trace v1.1 | 工具触迹记账 → 触迹/2026-09-02.jsonl | ✅ 动态挂载，已落账 |
| 6 | du-todo v4 | Todo 四象限窗口（3081） | ✅ 独立脚本 WMI 分离启动，GUI+API 正常 |
| ＋ | zhihu skill / standard 底座 | 知乎 CLI 技能 / 完整编码 Agent | 底座本会话已有（创造模式同源）；zhihu 下次 preset 会话回归 |

前五件合并为一个动态插件 **dubase-2**（一次 define+run）；du-todo 因依赖 node:http 无法进动态环境，走独立脚本（cmd 窗口开着，关窗即停——老约定）。

**二、根因（这次是真抓到了）**

探针插件（duprob-1，已停）两刀定位：
- `standingKeyFor('du')` **校验通过**——preset 文件健康、可挂载，且目录自 08-29 22:27 零改动；
- 但本会话实际 composed preset = **「cordis」**。

结论：**不是 preset 坏了，是会话启动时你选的「渡」没有被应用**——会话按创造模式组合了。三连裸跑 + 文件零改动 + composition 校验通过，三点互证。GUI 选择→组合那一段的机制在渡的可见范围之外，属于你要吃透的 DSH 工程（已立总账 Q2：「吃透 DSH preset 机制」，含第一步排查方向）。

**三、注意事项**

- 下个会话若选「渡」，**先关掉 3081 独立窗口**（端口互斥），preset du-todo 才能接管；
- 本次动态挂载基线=挂载时刻，挂载前的对话不入归档（断档一天，如实标注）；
- dsh.txt §九 已落盘根因与处置（「勿动态重建」降级为应急路径）；preset 侧 README 镜像已同步；今日日志 §六、总账 Q2 已记。

**四、待你验证（§2.6）**

下条消息发出后看有没有 `[时间锚点]` 尾巴——有，六件就全齐了。
