[du-archive] 2026-09-02T215031+08:00 | role=assistant | blocks=reasoning,text,tool-call
────────────
实测反馈清晰：/write 通了，差三块——标题注入、直接选稿（不碰文件）、回答页支持。方案：后端给 dusync 家族加一个 `GET /du-drafts` 端点（列 待发布/ + 按名取原文，CORS 开），脚本 v0.2 直接拉清单选稿；标题解析放脚本侧（改脚本不用重发布插件）；回答页走「记住待注入稿→打开问题页→自动注入」的跨页流。先挂端点：
