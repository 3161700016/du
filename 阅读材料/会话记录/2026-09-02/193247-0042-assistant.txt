[du-archive] 2026-09-02T193247+08:00 | role=assistant | blocks=reasoning,text,tool-call
────────────
入库成功，但落点偏差——du-scan 的 target 是工作区相对路径（惯例要带 `阅读材料/` 前缀，我漏了），文件落在根目录了。移正位置，并顺手把同节 overview 也扒了（主文档+节导览，两件即止——结算耦合律，不自肥）：
