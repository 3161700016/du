[du-archive] 2026-09-17T214047+08:00 | role=assistant | blocks=reasoning,text,tool-call
────────────
结构全部拿到，可以精确注入了：词汇模块就是同构 `<p><span leaf="">` 行（条目头 `<strong>` 加粗、条目间空行分隔），语料组=三段纯文本行。写 fill-w01-v2.js（沿用 09-15 的「未命中即报错」纪律，不许静默跳过）：
