[du-archive] 2026-09-17T213835+08:00 | role=assistant | blocks=reasoning,text
────────────
两处修好，管线备好，**授权三件套等你**。

**[工作区] 2026-09-17 21:0x**

**① 插图手机塌缩——已修**。根因：容器 `width:auto; flex-basis 0` 在微信手机渲染器里不被信任，图片掉回自然尺寸、靠 `align-self:flex-end` 缩到右边（电脑编辑器 flex 正常所以你看着没事）。容器已改 `display:block; width:75%; margin:0 auto`——不依赖 flex 的居中四分之三宽，任何渲染器都稳。其余 10 处同款样式是贴纸容器，你手机截图里贴纸正常，实证无病，未动。

**② 模板残余——已清**。根因：组5 示例解析是**三段式** `<p>`，我的锚点只吃掉首段。已显式删除后两段，残留扫描清单扩容（`《公约》/儿童/联合国/官方译文用` 等）——读回复验全 0，标签平衡闭合（179/92/13），`fill-w01-v2.js` 重跑全 PASS，其余 14 期自动继承这两处修复。

**请复检**：中转站重开 `W01-大熊猫` → 手机预览看插图（应 ~3/4 屏居中）。

**③ 草稿箱直连管线已建成**：`sync-draftbox.mjs`——token 缓存 → **外链图逐张 `/media/uploadimg` 转微信域**（说明里的头号坑：外链图会被 draft/add 过滤，管线内置解决）→ 封面上传 → `draft/add` 进后台草稿箱；`--check` 体检模式已通；`--dry` 预演不落稿。凭据将存 `C:\Users\31617\.dsh\wx-mp.json`（仓库外，与 pexels-key 同纪律）。**发布始终人工**——管线只送到草稿箱。

**需要你授权的三件**：
1. **AppID + AppSecret**——开发者平台 → 我的业务 → 公众号 → 基础信息 → 开发密钥（⚠ AppSecret 只显示一次，拿到直接发我或存入上述路径）；
2. **IP 白名单**——加本机公网 IPv4（沙箱里 IPv4 探测不通；你浏览器开 ifconfig.me 抄第一个 IPv4 给我，或自己填；家用宽带会漂移，可按 `x.x.x.0/24` 段填）；
3. **权限确认**——接口权限页看「草稿管理 → 新增草稿」是否“有权限”（个人号有 48001 风险，以页面实测为准）。

已知风险照实说：成品 content 51.7K 字符，超出说明口径的 2 万——管线会先照实提交，报 48003 再议压缩方案。
