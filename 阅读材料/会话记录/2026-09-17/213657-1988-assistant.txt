[du-archive] 2026-09-17T213657+08:00 | role=assistant | blocks=reasoning,text
────────────
全部落定。

**[工作区] 2026-09-17 19:5x**

**搁置（落协议）**：dsh.txt §三-3 增搁置清单——目录.txt 补录、总账口径、37 件留滚、先问后动条款、du-flash V0、Soul 活页 A+B、重启三修催办：**不再盘点、不再催办、不再请确认，直到你提起**。灯光也改「设灯即送达」，不再每次问颜色。

**三件默认项，实况**：

| 服务 | 状态 |
|------|------|
| du-todo 3081 | ✅ HTTP 200，窗口已开 |
| 中转站 3082 | ✅ HTTP 200，窗口已开 |
| mobile-remote 组网 | ✅ **当场已通，无需动作**——0.0.0.0:3080 + 3090 桥都在听，蒲公英在跑，防火墙放行在（今天进程启动时 09-15 的 winnat 持久排除已生效） |

**固化进度**：mobile-remote 本就是 profile 级现役插件（已是固化态）；**du-base v1.0.2 已装入 profile**（inject 三依赖修复版实测在位，下次 DSH 重启六件全绿+装配回执——重启不催，自然发生即可）；du-todo 与中转站的插件级固化（入 du-base / duclip 并入）记为候选工程，建成前独立脚本就是默认形态。以上全部写入 dsh.txt §三-0.7 与 §九。

已执行动作写入 `对话进程日志/日志2026-09-17.txt` §八（含「3456」的索引解释，如有出入你纠正即可）；协议变更在 `protocols/dsh.txt`。Git 未动，随今日收束一并提交。

一个小提醒：中转站和 du-todo 现在是**无 git 保护的出库库+独立进程**，进程会随机器重启消亡——但 0.7 条款已让每次启动自查自动拉起，等于用协议补了自启。真正开机自启（Task Scheduler）仍在搁置清单外、待你哪天想要再说。
