[du-archive] 2026-09-17T211804+08:00 | role=assistant | blocks=reasoning,text
────────────
## [工作区] 零件入库完毕——1142 件组件级素材

### 分类整理结果
| 类 | 数量 | 说明 |
|-----|------|------|
| 文字框 | 271 | 内容卡/节气卡/引言框/Tips 框…… |
| 小标题标号 | 459 | 含数字标号（01/02…）、栏目题头、END 标记 |
| 分割线 | 412 | 纯样式线、图形分隔、节日件（端午/重阳…） |

拆分器首航：**平衡切分算法**（section 深度游走＋自适应层级），1142 件全部登记进 `组件\零件索引.json`（每件＝`<序号>-<语义>.html＋.json`：类型/色系/文本预览/来源）。合集间重叠去重 244 件；抽查 24 件平衡性 0 失败；命名补丁 70 件（纯数字件→“数字标号01”、Xiumi 占位件→“占位文字框”）。颜色全部标注“可改”——调用时按目标栏目配色走爱牙日换绿同款流程。

### 图片本体备份：299/306，抓到 7 张“过期”现行
306 张唯一图片后台备份完成，**299 张落盘**；7 张失败全部是 img.xiumi.us 返回 **404——秀米 CDN 已删除**（文史哲艺-青绿/雪莱套）。这就是你担心的过期，第一次被当场抓获。回捞路径明确：该套上学期发布过，已发布文章里存在 mmbiz 版，可从微信文章页回捞（记入待办）。另有 37 个下载件无扩展名（文件本体完好，探测盲区），下次启动自查统一改名。

### 沉淀的工具（下次直接复用）
- `split.js`——零件合集拆分器：你以后再粘“全是XX”类合集，加一行 JOBS 重跑即可
- `_job.ps1`——图片备份 worker（可重复运行，已有文件自动跳过）
- 三条工程教训已记日志：EncodedCommand 编码、PS5.1 无 BOM 编码、日志切 URL 别用位置 split（静默跳过比失败更危险）

组件库现在是三层齐全的状态：**模板层 22 套＋零件层 1142 件＋图片本体 299 张**。继续粘，或指定哪套/哪类零件先投入下一期实战。
