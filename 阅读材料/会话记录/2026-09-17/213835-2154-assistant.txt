[du-archive] 2026-09-17T213835+08:00 | role=assistant | blocks=reasoning,text
────────────
改完，终验全绿。

**[工作区] 2026-09-17 21:1x**

**插图已迁位定版**：原 flex 容器整链删除（就是它在手机上塌缩的根源），新插图块是**最简结构**——居中 section + `<img width:100%>`，无任何花活，data-ratio 保比例（936×608，Pexels 裁切源）。位置精确落在你要的链上：

```
导语 → 红色分割线 → 图片 → 「学习语料」
```

校验：位置链实测通过、标签平衡 177/177 闭合、13 img（12 贴纸原封）、示例残留 0、脚本 [PASS]。注入器三处锚点失误全被 fail-loud + 读回复验拦下（75% 陈旧状态、pexels-vs-mmbiz、模板里红分割线不止一条——首条在标题区后），已如实入日志。

**草稿箱**：搁置，`sync-draftbox.mjs` 文件保留在项目根，哪天有公网/固定 IP 了 `--check` 一跑就能续上。

**剩一件事，你的眼睛**：中转站重开 `W01-大熊猫` → 手机预览——这次图片是普通全宽图，没有容器可塌。比例和位置对了的话，W01 就算定版，15 期的流水线（md 文稿 → fill 脚本 → 校验 → 中转站目检）也就全部定型了。
