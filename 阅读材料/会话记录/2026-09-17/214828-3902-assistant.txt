[du-archive] 2026-09-17T214828+08:00 | role=assistant | blocks=reasoning,text,tool-call
────────────
说明文件信息量大——最关键一条踩坑：**外链图必须走 `/media/uploadimg` 换微信域 URL，否则正文图片会被过滤**（API 路径的铁律，也解释了粘贴路径的行为）；另有 40164 IP 白名单、AppSecret 只显示一次、草稿权限因号而异。这些决定了我能先建管线、凭证要向你拿。继续两个 bug 的定位——插图行的完整结构（查有没有兄弟节点）和第 3 处《公约》残余的藏身处：
