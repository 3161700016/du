[du-archive] 2026-09-05T004101+08:00 | role=assistant | blocks=text
────────────
**[工作区] 复发已修——两级根因都抓到了，现在远程就能用。**

**先答插件名字**（你在 170 个的列表里搜这三个）：
- **`du-base`** —— 六件基建固化包（patch insert 行；**修复前它是“在而不活”的静默失败态**，v1.0.1 修复后下次重启应以启用态出现）
- **`dsh-mobile-remote`** —— 手机远程插件（正常在跑，3090 桥活着）
- **`synapse`** —— 会话地图（08-27 你裁定停用，disabled 行）

那 170 个大概率是内建 bundle 的全量组合行；我们 patch 进去的自定义件就上面三个。**启用状态最可靠的判定不在列表里，在我这边**：`GET /du-sync` 的版本串（现在应显示 `dubase-5 dynamic stopgap`，你下次重启后应变成 `du-base profile v1.0.1`）。

**复发根因（如实记档，两级）**：
1. **主因是我写错了插件形态**——Cordis 包插件要求**命名导出**（`export const name / inject` + `export function apply(ctx, config)`），我照着 preset `.mjs` 的 `export default` 工厂风格写了。加载器找不到 `apply` → **静默跳过，零报错**。对照组就是 mobile-remote：它今晚装载成功，形状差分一比就现形。
2. **次因是 pnpm file: 硬链接**——我改源码断开了链接，`pnpm install` 以为没变直接跳过，副本一直是旧的。强制删除重装才刷新。

**现在的状态**：
- **本会话**：动态救急版六件已全挂（含垫片），**远程设备硬刷新立即可用**——不用等重启。
- **下次重启**：du-base v1.0.1 正式接管（版本串 `du-base profile v1.0.1` 一眼可辨），从此六件常驻、与 preset 解耦。

教训已入日志 §十二（静默跳过类故障的对照诊断法 + file: 依赖强刷纪律）。今晚账很大，要收束说一声，我走完整流程。
