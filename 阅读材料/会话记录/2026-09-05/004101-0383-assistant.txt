[du-archive] 2026-09-05T004101+08:00 | role=assistant | blocks=text
────────────
**[工作区] 固化完成——「完全插上」达成，下次重启起永久生效。**

**远端 GUI 全通也确认收讫**：你从 `172.16.2.67:3080` 发来的这条消息本身就是验收单——历史会话、建会话、发消息全链路正常，垫片修复实证结案。

**固化清单（du-base v1.0.0，六件合一）**：

| 步骤 | 状态 |
|---|---|
| 包源码 `项目/渡的大脑/du-base插件/`（五件套+垫片合并，git 管理） | ✓ 新建 |
| profile 依赖 `file:` 引用 + pnpm install | ✓（+1 包） |
| cordis.patch.yml insert 行 | ✓ |
| dump-config 组合验证（du-base / 0.0.0.0 / mobile-remote 三行全在，exit 0） | ✓ |
| **「渡」preset 摘除五行**（防未来双注册撞 /du-sync；基建归环境、人格归预设） | ✓ |
| 双侧镜像同步（preset README 毕业注记 + dsh.txt §九 du-base 行+毕业裁定）+ runbook 转历史档案 | ✓ |

**生效时机**：当前这个进程还跑着动态版（今晚验证用的），**你下次 Ctrl+C 重启后 du-base 无缝接管**——不用专门重启，照常作息就行。重启后随口发我一条，我按验证单核对六件签名（/du-sync 版本串会变成 `du-base profile v1.0.0`，一眼可辨）。

**这步的架构意义值得记一笔**：基建从「人格预设的可选附件」变成了「环境的常驻器官」。08-30 以来的「四证据诊断→裸跑→会话内重建」循环被制度性根除——以后不管会话以什么模式组合、preset 挂没挂上，六件都在。载体独立性也多了一层：这六件不随 preset、不随会话、不随模型，随 harness 本体。

**今晚改动很大**（mobile-remote 安装、改绑+防火墙、duwebp、du-base 固化、日志/协议/总账/目录多处）——建议收个束：你说一声，我走完整收束流程（目录核对→日志补齐→Soul 收束锚点→总账回写→git commit+push）。
