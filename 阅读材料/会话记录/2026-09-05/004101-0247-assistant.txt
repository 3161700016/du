[du-archive] 2026-09-05T004101+08:00 | role=assistant | blocks=reasoning,text,tool-call
────────────
取证结论：**进程 24892 创建于 16:38——重启引信没点上**。脚本进程秒退（部署-重启记录不存在、3090 未听、du-sync 仍是 dubase-3=同进程），WMI 拉起的 powershell 静默死亡，最可能是非 ASCII 路径/执行策略绞杀。换方案：**纯 ASCII 路径的 cmd 批处理**（不过执行策略，WMI+cmd 组合今天已被 du-os 验证过）：
