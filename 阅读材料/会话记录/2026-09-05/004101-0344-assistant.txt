[du-archive] 2026-09-05T004101+08:00 | role=assistant | blocks=text
────────────
**[工作区] 垫片已上线，三路验证全绿——去远程设备硬刷新复测。**

| 验证点 | 结果 |
|---|---|
| 回环首页 `127.0.0.1:3080` | 200 + polyfill ✓（你本地不受影响） |
| 虚拟 IP 首页 `172.16.2.67:3080` | 200 + polyfill ✓（队友路径） |
| 3090 手机桥 | 401 门禁完好 ✓（App 不受影响） |

**你现在做**：远程设备**硬刷新**（PC 浏览器 Ctrl+F5；手机浏览器关掉标签重开或清缓存）→ 重新试两件事：①新建会话选工作目录（刚才报 crypto 错的地方）②看历史会话列表出不出来。

**预期**：randomUUID 补上后建会话流程应该通；会话列表如果还是空的，那就是另一个独立问题——在 PC 端浏览器按 F12 → Console，把红色报错截给我（手机上不方便调试，用队友的电脑或你自己 PC 开虚拟 IP 地址测最方便）。

两个后续（不急，确认垫片有效后再动）：
1. **duwebp 现在是动态插件，重启就死**——确认有效后我把它升级成 profile 插件（和 mobile-remote 同通道装进 node_modules），比赛周随便重启都不丢。
2. **正式修法是 HTTPS 反代**（给 3080 套 TLS，浏览器进入安全上下文，randomUUID 原生可用）——mkcert 签内网 CA 装到三台设备，比赛周后有余力再做；polyfill 这条腿撑比赛够用。
