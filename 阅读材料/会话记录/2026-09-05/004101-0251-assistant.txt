[du-archive] 2026-09-05T004101+08:00 | role=assistant | blocks=reasoning,text
────────────
**引信已点燃并验证**（日志第一行 21:04:21 已落）——这次是纯 ASCII 路径 cmd 批处理，不经过执行策略。**约 20 秒后页面会断开，等 1 分钟刷新 http://127.0.0.1:3080。**

**刚才发生了什么**（一句话版）：上一发引信是真的没点上——WMI 拉起的 powershell 秒退（中文路径+执行策略嫌疑），进程 24892 从 16:38 活到现在没动过，所以插件没加载、3090 没监听、设置页自然没有新条目。你的判断完全正确。

**刷新后依次做：**
1. **PC 端验证**：设置页出现「**连接移动端设备**」= 插件加载成功（新会话里我也会按日志 §八清单复查 lanBridge:3090）
2. **电脑装蒲公英**：https://pgy.oray.com/download/ → 登录 → 创建/加入智能组网 → 记下虚拟 IP（172.16.x.x）
3. **手机装两件**：应用商店「蒲公英」（同账号同组网，**免死金牌三件套**：省电无限制+自启动+锁后台）；APK 从 https://github.com/201222-L/dsh-mobile-remote/releases/latest 下 `DSH-Remote-v3.x.x.apk`
4. **首连**：App → 重新配置连接 → 手动填 `http://<电脑蒲公英虚拟IP>:3090` + 口令 `cuPpaZx8Lx-uNM7lZINJyDsB7RDBc6zv`（别扫码，码里是局域网地址）
5. **验收**：手机关 WiFi 走流量 → 状态点变绿 → 发一句话收到流式回复 = 校园网隔离判定「可行」结案

**万一刷新后 DSH 没回来**（概率低，命令就是现在跑的这条）：手动开个 cmd 执行
`cd /d D:\DeepSeekHarness && node --import tsx/esm apps/cli/src/bin.ts web --no-open`

本次会话随重启终结——交接全在 `日志2026-09-04.txt` §八（重启后验证清单+基建重建路径）。下个会话见。
