[du-archive] 2026-09-04T214307+08:00 | role=assistant | blocks=reasoning,text,tool-call
────────────
好——现在就干。你问的「会不会影响启动」先给结论：**启动命令一个字不用改**。patch 在 profile 层，你那三行 PS 照旧跑；`127.0.0.1:3080` 继续有效（0.0.0.0 绑定包含回环），变化只是同一端口开始额外接受蒲公英虚拟网卡来的连接。唯一真实风险=webserver 拒绝改绑导致起不来（web 版文档说可以，未实测）——预案在重启脚本里带了自检+自动留痕，起不来我下个会话自动回滚。

先做两件侦察：dump-config 看 webserver 行的**完整现役字段**（patch 是整体替换，漏字段会炸），顺便用 MCP 实查蒲公英虚拟 IP（验证查询链路）：
