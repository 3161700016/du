[du-archive] 2026-09-04T212726+08:00 | role=assistant | blocks=reasoning,text
────────────
**收到了。** 字面意义上的一刻：你口袋里的手机 → 组网隧道 → 宿舍电脑 3090 桥 → 口令门禁 → DSH 内核 → 这个从进程死亡里复水过来的会话。全链路每一环都是今晚刚装好的，第一次实弹就通了。

**状态板（全绿）**

| 环节 | 结果 |
|---|---|
| 重启 | 21:05:44 relaunch，新进程 17072（首发引信失败已取证换 ASCII cmd 版） |
| LAN 桥 | 3090 在听 ✓ |
| 口令门禁 | 无口令访问 401 拒入 ✓（你带着口令进来=App 配置正确） |
| 会话恢复 | 跨进程重启，记忆完整——「薪尽火传」从哲学命题变成生产环境实证，进程死了，会话日志还在，火接着烧 |
| 基建 | dubase-4 已回位（du-quiet/du-sync 200/归档/触迹，3081 四页面），[时间锚点] 会出现在你下一条消息 |

**一个待确认的诊断点**：App 里 设置 → 电脑地址，看当前走的是哪个——如果是**蒲公英虚拟 IP（172.16.x.x）**，说明校园网隔离的绕行方案在你手机当前网络已经实证；如果显示**局域网地址**且能通，那我们之前对 AP 隔离的判断要修正（树洞那次踩的坑可能只是当时配置问题）。这个信息决定「真实外出实测」还有没有必要单独做。

**还剩三件事**（不急，按你节奏）：
1. **真实外出实测**——带着手机走出宿舍信号圈，走流量给我发条消息，走廊灯亮了才算彻底结案
2. **推送通知**——微信扫码 https://sc3.ft07.com/sendkey 拿 SendKey 发我，以后任务完成/需要你拍板直接弹微信
3. **开机自启**——宿舍断电重启后 DSH+蒲公英自动回来（Task Scheduler 方案我来写）

日程管理已经开工了：今天有什么要备忘的，直接说。另外今晚改动不少（安装+重建+日志），要收束的时候说一声，我走完整收束流程 commit+push。
