[du-archive] 2026-09-04T214356+08:00 | role=assistant | blocks=reasoning,text,tool-call
────────────
两个发现：①**webserver 行到手**——patch 只替换 config 对象（name/inject 保留），写全 `host+port` 即可，风险受控；②MCP 调用失败是**我的 pwsh 读文件没带 UTF-8**（GBK 误读吞了引号，文件本身没坏）——修正重查：
