[du-archive] 2026-09-04T210023+08:00 | role=assistant | blocks=text
────────────
**[工作区] 安装执行完毕，重启引信已点火（25 秒后本进程重启——页面马上会断开，属预期）**

**PC 端已就位**（全部落盘，重启后不丢）：

| 项 | 状态 |
|---|---|
| 插件 | dsh-mobile-remote **v3.1.1** 装入 profiles/web（file: 依赖，+37 包） |
| 配置 | cordis.patch.yml：/m 路由 + **LAN 桥 0.0.0.0:3090**（避让 3080）+ 强口令 |
| 预检 | dump-config ✓（组合校验通过）；3081 du-os 已腾位 |
| 重启 | 原命令行新窗口重启，日志在 项目/dsh-mobile-remote/部署-重启记录.txt |
| **口令** | `cuPpaZx8Lx-uNM7lZINJyDsB7RDBc6zv`（也在 patch 文件里；勿截图外传，泄露即改） |

**⏳ 现在等约 1 分钟**（tsx 冷启动稍慢），然后刷新 http://127.0.0.1:3080 ——设置页应多出「**连接移动端设备**」。

---

**📱 手机端你要装两个东西：**

1. **蒲公英**（组网，校园网隔离的绕行方案）
   - 电脑：https://pgy.oray.com/download/ 装 PC 客户端，登录后创建/加入「智能组网」，记下电脑虚拟 IP（形如 `172.16.x.x`）
   - 手机：应用商店搜「蒲公英」，**同一账号**加入**同一组网**
   - ⚠ 三件套免死金牌：省电策略「无限制」+ 自启动「允许」+ 最近任务锁定——否则息屏即断

2. **DSH Remote APK**（控制台本体，安卓）
   - 下载：GitHub Releases https://github.com/201222-L/dsh-mobile-remote/releases/latest → `DSH-Remote-v3.x.x.apk`（微信传手机安装，允许未知来源）
   - **首连不要扫码**（二维码带局域网地址，校园网隔离打不通）：设置 → 重新配置连接 → **手动填 `http://<电脑蒲公英虚拟IP>:3090` + 上面那串口令**
   - 连上后它会记住双地址（出门走蒲公英、回家走局域网自动切换）

**验收动作**：手机关 WiFi 走流量 → 首页状态点变绿 → 随便发一句话收到流式回复 = 全链路通，校园网隔离正式判定「可行」。

**可选（之后再说）**：推送通知——微信扫码 https://sc3.ft07.com/sendkey 拿 SendKey 给我，我配 pushUrls，以后任务完成/需要你拍板直接弹微信。

---

本次会话随重启终结，一切已交接给磁盘：`日志2026-09-04.txt` §八含重启后验证清单（lanBridge.listening、四证据 preset 判定、基建重建路径）。下个会话见，久阳——这回你在操场上也能叫得动宿舍里的我了。
