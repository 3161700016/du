[du-archive] 2026-09-04T214019+08:00 | role=assistant | blocks=reasoning,text
────────────
**[工作区]**

**一、蒲公英 MCP skill 已建并实测 ✓**

- 握手打通：`initialize` → session → `tools/list` 全通，**贝锐蒲公英云 MCP v1.2.0，13 个工具**——关键的是 `get_sdwan_network`（查组网/成员）和 `manage_sdwan_network_member`（加人/退人/设角色），比赛部署的网络层操作我以后直接调，不用你碰 App。
- 凭据存 `C:\Users\31617\.dsh\pgy-mcp.json`（DSH_HOME，git 仓库外，核心层纪律）；技能= `skills/蒲公英MCP/SKILL.md`（含纯 HTTP JSON-RPC 的 pwsh 模板，DSH 没有 MCP 客户端，我用流式 HTTP 直调）。
- 一个值得说的发现：服务端管理类工具内建**两段式确认**（remove/delete 必须先触发提示、用户答复后才能 confirm，工具描述明令 AI 禁止自动确认）——和渡栈默认关闭项完全同构，我落成了硬规则。所以以后我退你手机、加队友，每一步都会先报备再动手。

**二、数学建模共享 harness：可以实现，而且不用写插件、不用改 dsh 启动命令**

| 层 | 方案 | 状态 |
|---|---|---|
| 网络 | 蒲公英组网 3 成员（你 PC + 两队友，手机临时退出） | 机制已验证（你手机今晚就是这么连的）；成员增删我走 MCP |
| 访问 | **队友浏览器开 `http://<你的虚拟IP>:3080` = 完整 DSH GUI** | 唯一待改配置（见下） |

关键认知：队友**不需要跑自己的 dsh 来连你**——DSH 0.1.1-rc.2 的会话存储是本机的，没有“远程 dsh 接入别的会话库”这个能力，这条路写插件也得从内核改起，不值。但 GUI 天然是“多个客户端看同一个会话库”：他们开浏览器，看到的和你屏幕上完全一样，发消息、跑程序、改论文、看流式输出，全在你电脑上执行。

**唯一要动的一处配置**：webserver 现在只听 127.0.0.1（所以队友连不上），赛前我打个 patch 改绑——可以收紧到只绑蒲公英虚拟网卡 IP（比 0.0.0.0 干净）。patch 机制今晚已验证过（mobile-remote 就是这么装的），但 webserver 行改绑本身**未实测**，比赛前几天我做一次预演：改 patch→重启→我手机伪装队友实测→开机自启脚本一起落。

**协作模式建议**：主力用「各开各的会话、共享同一文件系统」——论文放一个共享项目目录，跑程序互不排队；需要讨论时再进同一个会话轮流开火（agent 是串行的，多人同时发消息会自动排队，不会打架）。手机 App 装到队友手机上当轻量入口也行（共用口令），可选。

**三件注意事项**：①模型额度三人共享你的 API key（限速和钱都走你的）；②GUI 本身无口令，安全边界=蒲公英设备认证——**蒲公英账号开二次验证**，它就是大门；③赛后我把 webserver 回滚到回环。

待办已挂总账。今晚动的东西不少（安装+重建+MCP skill+日志），要收束说一声，我走完整流程 commit+push。
